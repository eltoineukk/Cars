<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1"/>
        <title>{% block title %}The cars. A game of new Generation.{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}" />
            <link rel="stylesheet" href="{{ asset('css/custom.css') }}" />
        {% endblock %}

        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <header>
            <div class="row">
                <img id="headCar" class="col-sm-12" src="{{ asset('systemImages/Header.png') }}">
                <nav class="navbar navbar-inverse p-3 bg-warning text-dark col-sm-12">
                    <div class="container-fluid">
                        <div class="navbar-header">
                            <a class="navbar-brand" href="{{ path('home_page') }}">The cars. A game of new generation</a>
                        </div>
                        <ul class="nav navbar-nav navbar-right">
                            <li><a href="{{ path('register_page') }}"><span class="glyphicon glyphicon-user"></span>Register</a></li>
                            <li><a href="{{ path('login_page') }}"><span class="glyphicon glyphicon-log-in"></span>Login</a></li>
                        </ul>
                    </div>
                </nav>
        </header>
        <section id="body">
        {% block body %}{% endblock %}
        </section>
        {% block javascripts %}
            <script src="{{ asset('js/bootstrap.js') }}"></script>
        {% endblock %}
    </body>
</html>
